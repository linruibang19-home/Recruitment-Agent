# Chrome 扩展安装

1. 打开 `chrome://extensions`。
2. 开启右上角“开发者模式”。
3. 点击“加载已解压的扩展程序”。
4. 选择项目中的 `browser-extension` 文件夹。
5. 固定“招聘 Agent 只读采集器”，并保持本地后端与已登录的 BOSS 页面打开。

扩展只读取当前 BOSS 页面，不填写输入框、不发送消息。控制台批准草稿也不会自动执行平台发送。
